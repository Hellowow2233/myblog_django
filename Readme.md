# Django 博客系统

一个基于 Django 6.0 构建的简洁博客应用，支持文章的创建、编辑、发布和展示功能。

## 📋 项目简介

这是一个功能完善的博客系统，包含以下核心功能：
- 博客文章列表展示
- 文章详情查看
- 创建新文章
- 编辑已有文章
- 用户认证与授权
- 响应式界面设计

## 🛠️ 技术栈

- **框架**: Django 6.0.1
- **数据库**: SQLite3
- **语言**: Python 3.x
- **前端**: HTML + CSS
- **时区**: Asia/Shanghai
- **语言**: 简体中文 (zh-hans)

## 📁 项目结构

```
django_dome/
├── blog/                      # 博客应用
│   ├── models.py             # 数据模型 (Post)
│   ├── views.py              # 视图函数
│   ├── urls.py               # URL 路由
│   ├── forms.py              # 表单定义
│   ├── admin.py              # 后台管理
│   ├── templates/            # 模板文件
│   │   └── blog/
│   │       ├── base.html     # 基础模板
│   │       ├── index.html    # 首页
│   │       ├── post_list.html    # 文章列表
│   │       ├── post_detail.html  # 文章详情
│   │       └── post_edit.html    # 编辑页面
│   └── static/               # 静态文件
│       └── css/
│           └── blog.css
├── mysite/                   # 项目配置
│   ├── settings.py           # 项目配置
│   ├── urls.py               # 主路由
│   └── wsgi.py               # WSGI 配置
├── manage.py                 # Django 管理脚本
├── db.sqlite3                # SQLite 数据库
└── Readme.md                 # 项目文档
```

## 🚀 快速开始

### 环境要求

- Python 3.8+  （项目实际测试环境为3.13）
- pip

### 安装步骤

1. **克隆项目**
```bash
git clone <repository-url>
cd django_dome
```

2. **创建虚拟环境**
```bash
python -m venv .venv
```

3. **激活虚拟环境**
```bash
# Windows
.venv\Scripts\Activate.ps1

# Linux/Mac
source .venv/bin/activate
```

4. **安装依赖**
```bash
pip install django
```

5. **数据库迁移**
```bash
python manage.py makemigrations
python manage.py migrate
```

6. **创建超级用户**
```bash
python manage.py createsuperuser # 按提示完成后续操作
```

7. **运行开发服务器**
```bash
python manage.py runserver
```

8. **访问应用**
- 博客首页: http://127.0.0.1:8000/
- 后台管理: http://127.0.0.1:8000/admin/

## 📌 主要功能

### 数据模型

**Post (博客文章)**
- `author`: 作者 (外键关联 User)
- `title`: 标题
- `text`: 正文内容
- `created_date`: 创建时间
- `published_date`: 发布时间

### URL 路由

| URL | 功能 | 视图 |
|-----|------|------|
| `/` | 文章列表 | `post_list` |
| `/home/` | 首页 | `index` |
| `/post/<pk>` | 文章详情 | `post_detail` |
| `/post/new/` | 创建文章 | `post_new` |
| `/post/<pk>/edit/` | 编辑文章 | `post_edit` |
| `/admin/` | 后台管理 | Django Admin |

## 🔐 安全提示

⚠️ **重要**: 本项目包含开发环境配置，编辑 `mysite/settings.py` 内容，在生产环境部署时请注意：

1. 修改 `SECRET_KEY` 为安全的随机字符串
2. 设置 `DEBUG = False`
3. 配置正确的 `ALLOWED_HOSTS`
4. 使用生产级数据库（如 PostgreSQL、MySQL）
5. 配置静态文件服务
6. 启用 HTTPS

## 🌐 部署支持

项目已配置 PythonAnywhere 部署支持，ALLOWED_HOSTS 包含：
- `sloth.pythonanywhere.com`
- `127.0.0.1`
- `localhost`
若要部署请自行修改 ALLOWED_HOST

## 📝 开发说明

### 创建新文章
1. 登录系统（需要用户权限）
2. 访问 `/post/new/`
3. 填写标题和内容
4. 提交后自动发布

### 编辑文章
1. 访问文章详情页
2. 点击编辑链接或访问 `/post/<id>/edit/`
3. 修改内容后保存

## 🤝 贡献

欢迎提交 Issue 和 Pull Request！

## 📄 许可证

本项目仅供学习和研究使用。[对应教程](https://tutorial.djangogirls.org/zh/)

## 📧 联系方式

如有问题或建议，欢迎联系项目维护者。

---

**Django版本**: 6.0.1
